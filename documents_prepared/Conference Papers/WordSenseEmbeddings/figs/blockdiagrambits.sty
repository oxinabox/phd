\NeedsTeXFormat{LaTeX2e}[1994/06/01]
\ProvidesPackage{blockdiagrambits}
[2016/10/29 v0.01 LaTeX package defining shapes etc for drawing block diagrams using tikz]

\RequirePackage{tikz}
\RequirePackage{xargs}


\usetikzlibrary{positioning}
\usetikzlibrary{shapes} 
\usetikzlibrary{arrows.meta}
\usetikzlibrary{decorations.pathmorphing}

\tikzset{
	backgroundcolor/.style ={fill=white}	
}

%\DeclareOption*{%
%	\typeout{\CurrentOption}
%	\tikzset{backgroundcolor/.style ={fill=\CurrentOption}}}%
%\ProcessOptions\relax

\tikzset{
	tripleinner/.style args={[#1] in [#2] in [#3]}{
		#1,preaction={preaction={draw,#3},draw,#2}
	},
	triple/.style={%
		tripleinner={[line width=0.5pt,black] in
			[line width=3pt,white, arrows={-Classical TikZ Rightarrow[line width=0.5, black, width=13]}] in
			[line width=4pt,black, shorten >=2.9pt,arrows={-Circle[scale=0.1]}]}	 
	}
}

\tikzset{
	input/.style={
		%circle,
		%draw,
		Blue,
		minimum width = 2
	},
	datastore/.style={cylinder,
		draw,
		Green,
		minimum width = 2
	},
	proc/.style={rectangle,
		draw,
		Red,
		minimum size = 50
	},
	link/.style={->,
		black},
	multi/.style={%-Implies,
		triple,
	},
	labe/.style={
		Blue,
		backgroundcolor,
		fill opacity=0.6,
		text opacity=1,
		font={\footnotesize\itshape}	
	}
}


%http://tex.stackexchange.com/a/230151/5834
\newlength{\myline}% line thickness
\setlength{\myline}{0.5pt}

\newcommandx*{\triplearrow}[4][1=0, 2=1]{% #1 = shorten left (optional), #2 = shorten right (optionsl),
	% #3 = draw options (must contain arrow type), #4 = path
	\draw[line width=\myline,double distance=6\myline,#3] #4;
	\draw[line width=\myline,shorten <=#1\myline,shorten >=#2\myline,#3] #4;
}

\newcommandx*{\quadarrow}[4][1=0, 2=2.5]{% #1 = shorten left (optional), #2 = shorten right (optionsl),
	% #3 = draw options (must contain arrow type), #4 = path
	\draw[line width=\myline,double distance=5\myline,#3] #4;
	\draw[line width=\myline,double distance=\myline,shorten <=#1\myline,shorten >=#2\myline,#3] #4;
}




\endinput